<header class="p-4 border-b border-slate-800 mb-6">
  <nav class="flex items-center gap-6">

    <!-- Logo / Home -->
    <a href="/" class="text-xl font-semibold text-indigo-400 hover:text-indigo-300">
      Double
    </a>

    <!-- Main navigation -->
    <a href="/ads" class="text-slate-300 hover:text-slate-100">Browse Ads</a>

    <% if (userId) { %>
      <a href="/ads/new" class="text-slate-300 hover:text-slate-100">Post Ad</a>
      <a href="/logout" class="text-slate-400 hover:text-slate-200 ml-auto">Logout</a>
    <% } else { %>
      <div class="ml-auto flex gap-4">
        <a href="/login" class="text-slate-400 hover:text-slate-200">Login</a>
        <a href="/signup" class="text-slate-400 hover:text-slate-200">Signup</a>
      </div>
    <% } %>

  </nav>
</header>


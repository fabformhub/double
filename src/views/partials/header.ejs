<nav class="w-full border-b border-slate-800/60 bg-slate-900/40 backdrop-blur-xl shadow-lg shadow-black/20">
  <div class="max-w-7xl mx-auto px-6 py-5 flex items-center justify-between">

    <!-- Brand (Two-Tone Premium Logo) -->
    <a href="/" class="group flex items-center">
      <span class="logo-wrapper text-3xl font-extrabold tracking-tight transition-all duration-500">
        <span class="logo-nooky">Nooky</span><span class="logo-up">Up</span>
      </span>
    </a>

    <!-- Desktop Nav -->
    <div class="hidden md:flex items-center gap-10 text-lg font-medium text-slate-300">

      <a 
        href="/" 
        class="
          nookyup-hover
          px-2 py-1 rounded-lg
          transition-all duration-500
          hover:text-pink-300
          hover:scale-[1.05]
        "
      >
        Home
      </a>

      <% if (!userId) { %>

        <a 
          href="/login" 
          class="
            nookyup-hover
            px-2 py-1 rounded-lg
            transition-all duration-500
            hover:text-pink-300
            hover:scale-[1.05]
          "
        >
          Login
        </a>

        <a 
          href="/signup" 
          class="
            nookyup-hover
            px-2 py-1 rounded-lg
            transition-all duration-500
            hover:text-pink-300
            hover:scale-[1.05]
          "
        >
          Sign Up
        </a>

      <% } else { %>

        <a 
          href="/dashboard" 
          class="
            nookyup-hover
            px-2 py-1 rounded-lg
            transition-all duration-500
            hover:text-pink-300
            hover:scale-[1.05]
          "
        >
          Dashboard
        </a>

        <a 
          href="/logout" 
          class="
            nookyup-hover
            px-2 py-1 rounded-lg
            transition-all duration-500
            hover:text-pink-300
            hover:scale-[1.05]
          "
        >
          Logout
        </a>

      <% } %>
    </div>

    <!-- Mobile Menu Button -->
    <button 
      id="mobileMenuBtn" 
      class="
        md:hidden text-3xl text-slate-300 
        transition-all duration-300 
        hover:text-pink-400
        hover:scale-[1.1]
      "
    >
      ☰
    </button>
  </div>

  <!-- Mobile Menu -->
  <div 
    id="mobileMenu" 
    class="
      hidden md:hidden px-6 pb-6 flex flex-col gap-4 text-lg font-medium 
      text-slate-300 bg-slate-900/80 backdrop-blur-xl 
      border-t border-slate-800/60
    "
  >
    <a href="/" class="nookyup-hover px-2 py-1 rounded-lg hover:text-pink-300 transition-all duration-500">Home</a>

    <% if (!userId) { %>
      <a href="/login" class="nookyup-hover px-2 py-1 rounded-lg hover:text-pink-300 transition-all duration-500">Login</a>
      <a href="/signup" class="nookyup-hover px-2 py-1 rounded-lg hover:text-pink-300 transition-all duration-500">Sign Up</a>
    <% } else { %>
      <a href="/dashboard" class="nookyup-hover px-2 py-1 rounded-lg hover:text-pink-300 transition-all duration-500">Dashboard</a>
      <a href="/logout" class="nookyup-hover px-2 py-1 rounded-lg hover:text-pink-300 transition-all duration-500">Logout</a>
    <% } %>
  </div>
</nav>

<!-- Mobile Menu Script -->
<script>
  const btn = document.getElementById("mobileMenuBtn");
  const menu = document.getElementById("mobileMenu");

  btn?.addEventListener("click", () => {
    menu.classList.toggle("hidden");
  });
</script>

<!-- Two-Tone Logo Styles -->
<style>
  .logo-wrapper {
    display: inline-flex;
    gap: 0;
    letter-spacing: -0.5px;
  }

  /* Nooky — seductive pink-magenta gradient */
  .logo-nooky {
    background: linear-gradient(90deg, #ec4899, #f472b6);
    -webkit-background-clip: text;
    color: transparent;
    filter: drop-shadow(0 0 10px rgba(236,72,153,0.7));
    transition: all 0.5s ease;
  }

  /* Up — electric violet-blue gradient */
  .logo-up {
    background: linear-gradient(90deg, #8b5cf6, #6366f1);
    -webkit-background-clip: text;
    color: transparent;
    margin-left: 2px;
    filter: drop-shadow(0 0 10px rgba(139,92,246,0.7));
    transition: all 0.5s ease;
  }

  /* Hover glow intensifies */
  .group:hover .logo-nooky {
    filter: drop-shadow(0 0 14px rgba(236,72,153,1));
    transform: translateY(-1px);
  }

  .group:hover .logo-up {
    filter: drop-shadow(0 0 14px rgba(139,92,246,1));
    transform: translateY(-1px);
  }
</style>


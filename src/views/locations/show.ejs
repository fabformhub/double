<div class="max-w-4xl mx-auto py-16 px-6">

  <h1 class="text-4xl font-semibold mb-10 tracking-tight">
    <span class="text-pink-600"><%= location.city_name %></span>
  </h1>

  <p class="text-lg text-gray-600 mb-12">
    Choose a category.
  </p>

  <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
    <% categories.forEach(cat => { %>
      <a
        href="/<%= location.country_code %>/<%= location.slug %>/<%= cat.slug %>"
        class="flex items-center gap-3 text-xl text-gray-800 hover:text-pink-600 transition"
      >
        <span>📂</span>
        <span><%= cat.name %></span>
      </a>
    <% }) %>
  </div>

</div>

